'use client';import {useState} from 'react';import {Mail,Sparkles,ArrowRight} from 'lucide-react';
export default function Login(){const[email,setEmail]=useState('');const[code,setCode]=useState('');const[step,setStep]=useState<'email'|'code'>('email');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[devCode,setDevCode]=useState('');async function submit(){setBusy(true);setError('');const url=step==='email'?'/api/auth/request-code':'/api/auth/verify';const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(step==='email'?{email}:{email,code})});const d=await r.json();setBusy(false);if(!r.ok)return setError(d.error);if(step==='email'){setStep('code');if(d.devCode)setDevCode(d.devCode)}else location.reload();}return <main className="login"><section className="login-copy"><div className="brand"><span>🐾</span> Pawborough</div><div className="eyebrow"><Sparkles size={14}/> A little world with a big heart</div><h1>Meet your new<br/><em>best friend.</em></h1><p>Design a one-of-a-kind pet, make the coziest home in town, and fill every day with tiny adventures.</p><div className="login-card"><h2>{step==='email'?'Come on in!':'Check your inbox'}</h2><p>{step==='email'?'No passwords. We’ll email you a magic code.':`We sent a six-digit code to ${email}`}</p>{step==='email'?<label><Mail size={18}/><input autoFocus type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)} onKeyDown={e=>e.key==='Enter'&&submit()}/></label>:<input className="code" autoFocus inputMode="numeric" maxLength={6} placeholder="000000" value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))} onKeyDown={e=>e.key==='Enter'&&submit()}/>} {devCode&&<button className="dev-code" onClick={()=>setCode(devCode)}>Development code: {devCode}</button>}{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy} onClick={submit}>{busy?'One moment…':step==='email'?'Send my code':'Enter Pawborough'} <ArrowRight size={18}/></button>{step==='code'&&<button className="text-btn" onClick={()=>setStep('email')}>Use a different email</button>}</div></section><div className="login-art"><div className="float-card one">🎣 <b>Rare catch!</b><span>Moon Koi</span></div><div className="float-card two">🏆 <b>Level up!</b><span>New room unlocked</span></div></div></main>}
